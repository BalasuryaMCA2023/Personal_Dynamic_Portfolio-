const express = require("express");
const router = express.Router();

// No user-side authentication required for view-only portfolio.
// Admin authentication is handled by the dedicated Admin Backend.

module.exports = router;

